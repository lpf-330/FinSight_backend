# FinSight 错误修复方案

> 修复日期：2026-04-29
> 待修复错误：ERR-007, ERR-008, ERR-012, ERR-013, ERR-016, ERR-022

---

## 1. 错误汇总

| 错误ID | 影响接口 | 问题描述 | 错误类型 |
|--------|----------|----------|----------|
| ERR-007 | GET /api/algorithm/warning/records | 预警记录查询返回500 | 数据库表缺失 |
| ERR-008 | PUT /api/algorithm/warning/records/:id/ignore | 忽略预警返回500 | 数据库表缺失 |
| ERR-012 | POST /api/report/pdf/generate | PDF报告生成返回500 | 数据库表缺失 |
| ERR-013 | GET /api/report/pdf/history | PDF历史查询返回500 | 数据库表缺失 |
| ERR-016 | POST /api/algorithm/investment/scheme | 投资方案中文乱码 | 数据库字符集配置 |
| ERR-022 | POST /api/config/formula/threshold/knowledge | 创建记录返回id为null | 自增ID配置问题 |

---

## 2. 详细修复方案

### 2.1 ERR-007 & ERR-008 - 预警记录表缺失

**问题分析**：
- [WarningRecordMapper.java](file:///c:\Users\yzyxt\Desktop\金融财务分析平台\FinSight_backend\src\main\java\com\sixth_group\finsight_backend\mapper\WarningRecordMapper.java) 代码正确
- 查询的表 `algo_warning_record` 在数据库中不存在
- 参考 [database-design.md](file:///c:\Users\yzyxt\Desktop\金融财务分析平台\FinSight_backend\docs\design\database\database-design.md#L286-L305) 中表结构定义

**修复方案**：
执行以下SQL创建预警记录表：

```sql
CREATE TABLE IF NOT EXISTS algo_warning_record (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    indicator VARCHAR(64) NOT NULL COMMENT '预警指标名称',
    indicator_code VARCHAR(16) NOT NULL COMMENT '指标编码',
    level ENUM('yellow','orange','red') NOT NULL COMMENT '预警级别',
    current_value DECIMAL(18,4) COMMENT '当前值',
    yellow_threshold VARCHAR(32) COMMENT '黄色阈值',
    orange_threshold VARCHAR(32) COMMENT '橙色阈值',
    red_threshold VARCHAR(32) COMMENT '红色阈值',
    trend VARCHAR(64) COMMENT '趋势描述',
    suggestion TEXT COMMENT '调整建议',
    status ENUM('active','ignored') NOT NULL DEFAULT 'active' COMMENT '状态',
    period VARCHAR(16) NOT NULL COMMENT '期间',
    triggered_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '触发时间',
    INDEX idx_status_level (status, level),
    INDEX idx_triggered_at (triggered_at DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='预警记录表';
```

---

### 2.2 ERR-012 & ERR-013 - PDF报告表缺失

**问题分析**：
- [PdfReportMapper.java](file:///c:\Users\yzyxt\Desktop\金融财务分析平台\FinSight_backend\src\main\java\com\sixth_group\finsight_backend\mapper\PdfReportMapper.java) 代码正确，已添加 `@Options(useGeneratedKeys = true, keyProperty = "id")`
- 查询的表 `report_pdf_report` 在数据库中可能不存在
- 参考 [database-design.md](file:///c:\Users\yzyxt\Desktop\金融财务分析平台\FinSight_backend\docs\design\database\database-design.md#L367-L385) 中表结构定义

**修复方案**：
执行以下SQL创建PDF报告表：

```sql
CREATE TABLE IF NOT EXISTS report_pdf_report (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(256) NOT NULL COMMENT '报告名称',
    type ENUM('comprehensive','warning','investment') NOT NULL COMMENT '报告类型',
    period VARCHAR(16) COMMENT '分析期间',
    include_warning TINYINT(1) DEFAULT 0 COMMENT '包含预警信息',
    include_ratio TINYINT(1) DEFAULT 1 COMMENT '包含比率分析',
    include_trend TINYINT(1) DEFAULT 1 COMMENT '包含趋势图表',
    include_investment TINYINT(1) DEFAULT 0 COMMENT '包含投资评估',
    status ENUM('completed','generating','failed') NOT NULL DEFAULT 'generating' COMMENT '状态',
    file_path VARCHAR(512) COMMENT '文件存储路径',
    file_size VARCHAR(32) COMMENT '文件大小',
    creator_id BIGINT COMMENT '生成人ID',
    creator_name VARCHAR(64) COMMENT '生成人(冗余)',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '生成时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='PDF报告表';
```

---

### 2.3 ERR-016 - 投资方案中文乱码

**问题分析**：
- [InvestmentSchemeMapper.java](file:///c:\Users\yzyxt\Desktop\金融财务分析平台\FinSight_backend\src\main\java\com\sixth_group\finsight_backend\mapper\InvestmentSchemeMapper.java) 代码正确
- 项目数据库设计文档指定字符集为 `utf8mb4`
- 问题根源：数据库连接字符串未指定字符集或数据库本身字符集配置不正确

**修复方案**（按优先级选择）：

**方案A - 检查数据库连接配置**（推荐）：
检查 `application.yml` 或 `application.properties` 中的数据库连接URL，确保包含以下参数：
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/finsight?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
```

**方案B - 确认数据库字符集**：
执行以下SQL检查数据库字符集：
```sql
SHOW CREATE DATABASE finsight;
```
如果显示的不是 `utf8mb4`，需要修改：
```sql
ALTER DATABASE finsight CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

**方案C - 确认表字符集**：
```sql
ALTER TABLE algo_investment_scheme CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

---

### 2.4 ERR-022 - 创建记录返回id为null

**问题分析**：
- [FormulaMapper.java](file:///c:\Users\yzyxt\Desktop\金融财务分析平台\FinSight_backend\src\main\java\com\sixth_group\finsight_backend\mapper\FormulaMapper.java) 第62行已添加 `@Options(useGeneratedKeys = true, keyProperty = "id")`
- [ThresholdMapper.java](file:///c:\Users\yzyxt\Desktop\金融财务分析平台\FinSight_backend\src\main\java\com\sixth_group\finsight_backend\mapper\ThresholdMapper.java) 第61行已添加 `@Options` 注解
- [KnowledgeMapper.java](file:///c:\Users\yzyxt\Desktop\金融财务分析平台\FinSight_backend\src\main\java\com\sixth_group\finsight_backend\mapper\KnowledgeMapper.java) 第69行已添加 `@Options` 注解
- 代码层面已修复，问题在于数据库表结构的ID字段未配置自增

**修复方案**：

**检查表结构**：
```sql
-- 检查 config_formula 表结构
DESCRIBE config_formula;
-- 或者
SHOW CREATE TABLE config_formula;
```

**如果ID字段没有AUTO_INCREMENT，执行以下SQL**：
```sql
-- 修改 config_formula 表
ALTER TABLE config_formula MODIFY COLUMN id BIGINT AUTO_INCREMENT;

-- 修改 config_threshold 表
ALTER TABLE config_threshold MODIFY COLUMN id BIGINT AUTO_INCREMENT;

-- 修改 config_knowledge 表
ALTER TABLE config_knowledge MODIFY COLUMN id BIGINT AUTO_INCREMENT;
```

**如果表不存在，需要创建表**（参考 [database-design.md](file:///c:\Users\yzyxt\Desktop\金融财务分析平台\FinSight_backend\docs\design\database\database-design.md#L183-L227)）：
```sql
CREATE TABLE IF NOT EXISTS config_formula (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(128) NOT NULL COMMENT '公式名称',
    expression TEXT NOT NULL COMMENT '表达式',
    category VARCHAR(32) NOT NULL COMMENT '分类',
    enabled TINYINT(1) NOT NULL DEFAULT 1 COMMENT '是否启用',
    version VARCHAR(16) NOT NULL COMMENT '版本号',
    created_by VARCHAR(64) COMMENT '创建人',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='公式配置表';

CREATE TABLE IF NOT EXISTS config_threshold (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    indicator VARCHAR(64) NOT NULL COMMENT '预警指标名称',
    indicator_code VARCHAR(16) NOT NULL COMMENT '指标编码',
    direction ENUM('lower','upper') NOT NULL DEFAULT 'lower' COMMENT '判断方向',
    yellow_value VARCHAR(32) NOT NULL COMMENT '黄色阈值',
    orange_value VARCHAR(32) NOT NULL COMMENT '橙色阈值',
    red_value VARCHAR(32) NOT NULL COMMENT '红色阈值',
    dynamic_base TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否启用动态基准',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='预警阈值配置表';

CREATE TABLE IF NOT EXISTS config_knowledge (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    indicator VARCHAR(64) NOT NULL COMMENT '预警指标名称',
    level ENUM('yellow','orange','red') NOT NULL COMMENT '预警级别',
    content TEXT NOT NULL COMMENT '建议内容',
    version VARCHAR(16) NOT NULL COMMENT '版本号',
    useful_count INT DEFAULT 0 COMMENT '有用反馈数',
    useless_count INT DEFAULT 0 COMMENT '无用反馈数',
    created_by VARCHAR(64) COMMENT '创建人',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_indicator_level (indicator, level)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识库表';
```

---

## 3. 修复优先级建议

| 优先级 | 错误ID | 说明 |
|--------|--------|------|
| 高 | ERR-007, ERR-008 | 创建预警记录表，解决算法模块核心功能 |
| 高 | ERR-012, ERR-013 | 创建PDF报告表，解决报表模块核心功能 |
| 中 | ERR-016 | 数据库字符集配置，影响中文数据存储 |
| 中 | ERR-022 | 自增ID配置，影响配置模块CRUD功能 |

---

## 4. 修复后验证

执行以下SQL验证表创建成功：
```sql
-- 验证表是否存在
SHOW TABLES LIKE 'algo_warning_record';
SHOW TABLES LIKE 'report_pdf_report';
SHOW TABLES LIKE 'config_formula';
SHOW TABLES LIKE 'config_threshold';
SHOW TABLES LIKE 'config_knowledge';

-- 验证自增ID配置
SHOW COLUMNS FROM config_formula FROM finsight WHERE Field = 'id';
-- 应该显示 Extra 列包含 auto_increment
```

执行接口测试验证功能正常：
```bash
# 测试预警记录接口
curl -X GET http://localhost:8080/api/algorithm/warning/records -H "Authorization: Bearer <token>"

# 测试PDF报告生成
curl -X POST http://localhost:8080/api/report/pdf/generate -H "Authorization: Bearer <token>" -d '{"name":"测试报告","type":"comprehensive"}'

# 测试投资方案保存
curl -X POST http://localhost:8080/api/algorithm/investment/scheme -H "Authorization: Bearer <token>" -d '{"projectName":"测试项目"}'

# 测试配置创建
curl -X POST http://localhost:8080/api/config/formula -H "Authorization: Bearer <token>" -d '{"name":"测试公式","expression":"a/b","category":"solvency"}'
```

---

## 5. 相关文件清单

| 文件路径 | 说明 |
|----------|------|
| `mapper/WarningRecordMapper.java` | 预警记录Mapper，已验证正确 |
| `mapper/PdfReportMapper.java` | PDF报告Mapper，已验证正确 |
| `mapper/InvestmentSchemeMapper.java` | 投资方案Mapper，已验证正确 |
| `mapper/FormulaMapper.java` | 公式Mapper，已添加@Options注解 |
| `mapper/ThresholdMapper.java` | 阈值Mapper，已添加@Options注解 |
| `mapper/KnowledgeMapper.java` | 知识库Mapper，已添加@Options注解 |
| `docs/design/database/database-design.md` | 数据库设计文档（表结构参考） |
| `docs/test report/错误修复报告.md` | 错误修复报告 |

---

**方案编制完成，待DBA执行数据库脚本后即可解决全部6个错误。**